# frozen_string_literal: true

module SseCable
  class Connection
    def initialize(env)
      # Initialize connection with environment
    end

    def transmit(event)
      # Transmit event data to the client
    end
  end
end
