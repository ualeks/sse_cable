# frozen_string_literal: true

module SseCable
  class Middleware
    def initialize(app)
      # Initialize middleware with app
    end

    def call(env)
      # Handle SSE requests in middleware
    end
  end
end
